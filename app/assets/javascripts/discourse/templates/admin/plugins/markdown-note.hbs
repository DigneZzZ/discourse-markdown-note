<div class="admin-container">
  <div class="admin-title">
    <h1>Настройки заметок Markdown</h1>
  </div>
  
  <div class="admin-content">
    <div class="markdown-note-settings">
      <p class="description">Настройте цвета и внешний вид различных типов заметок. Изменения применятся сразу же.</p>
      
      <div class="note-type-settings">
        {{#each noteTypes as |type|}}
          <div class="note-type-section">
            <h3>{{type.icon}} {{type.name}}</h3>
            
            <div class="color-settings">
              <div class="color-input-group">
                <label>Цвет фона</label>
                <input type="color" 
                       class="color-picker" 
                       data-setting="{{type.key}}_bg_color"
                       value="{{get noteSettings (concat type.key '_bg_color')}}"
                       {{action "updateColor"}}>
              </div>
              
              <div class="color-input-group">
                <label>Цвет границы</label>
                <input type="color" 
                       class="color-picker" 
                       data-setting="{{type.key}}_border_color"
                       value="{{get noteSettings (concat type.key '_border_color')}}"
                       {{action "updateColor"}}>
              </div>
              
              <div class="color-input-group">
                <label>Цвет текста</label>
                <input type="color" 
                       class="color-picker" 
                       data-setting="{{type.key}}_text_color"
                       value="{{get noteSettings (concat type.key '_text_color')}}"
                       {{action "updateColor"}}>
              </div>
            </div>
            
            <div class="preview-section">
              <h4>Предпросмотр</h4>
              <div class="note-preview {{type.key}}" data-preview-type="{{type.key}}">
                <span class="note-icon">{{type.icon}}</span>
                <div class="note-content">
                  Это пример {{type.name}} с настраиваемыми цветами.
                </div>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
      
      <div class="admin-actions">
        <button type="button" class="btn btn-primary save-settings" {{action "saveSettings"}}>
          {{#if saving}}Сохранение...{{else}}Сохранить настройки{{/if}}
        </button>
        <button type="button" class="btn btn-default reset-settings" {{action "resetDefaults"}}>
          Сбросить к умолчанию
        </button>
      </div>
      
      <div class="admin-status"></div>
    </div>
  </div>
</div>
